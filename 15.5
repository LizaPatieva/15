#include <iostream> //Подключение библиотеки iostream
using namespace std;
float fact2 (int n) //Создаем функцию
{
  int t=1; //Задаем переменную
  for (;n>0; n-=2)
  t *=n;
  return t;
}
  int main (void) //Точка входа в программу
{
  int i, n; //Задаем переменную
  for (i=1; i<=5; ++i)
{
  printf ("N:"); //Выводим на экран
  scanf ("%i", &n); //Вводим значение n
  printf ("fact: %f\n", fact2(n)); //Выводим результат на экран
}
return 0; //Возврат целочисленного значения перед завершением функции
}
