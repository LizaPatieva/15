#include <iostream> //Подключение библиотеки iostream
void power (float A, float *B) //Создаем функцию, выcчитывающую третью степень чисел
{
 *B=A*A*A; //формула нахождения 3-ей степени чисел
}
  int main(void) //Точка входа в программу
 {
  for (int i=1; i<=5; ++i) //Создаем цикл для расчета третьей степени 5 чисел
 {
  float A; //Назначение переменной a
  printf("A:"); //Выводим на экран
  scanf ("%f", &A); Вводим значение переменной a
  float B; //Назначение переменной B
  power (a, &B); //Выводим на экран
  printf("B: %f\n", B); //Выводим на экран
 }
return 0;  //Возврат целочисленного значения перед завершением функции
}
